apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: default
spec:
  parentRefs:
  - name: external-http
  hostnames:
  - ${domain}
  rules:
  - matches:
    - path:
        value: /
    backendRefs:
    - name: keycloak
      port: 8080