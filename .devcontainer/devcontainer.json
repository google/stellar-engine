{
  "name": "Stellar Engine Workstation",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "postCreateCommand": "pip3 install --no-cache-dir --break-system-packages -r tools/requirements.txt",
  "remoteEnv": {
    "CLOUDSDK_CORE_DISABLE_PROMPTS": "1"
  }
}
